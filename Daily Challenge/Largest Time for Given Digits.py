from typing import List

class Solution:
    def largestTimeFromDigits(self, arr: List[int]) -> str:
        out = ""
        for n in sorted(arr):
            

print(Solution().largestTimeFromDigits())